<div class="container">
  
  <div class="row mt-3">
    <h2>成分編集</h2>
  </div>
  
  <div class="row mt-3">
    <%= form_with model: @ingredient, url: admin_ingredient_path, local: true do |f| %>
    <table>
      <tr>
        <th><%= f.label "成分名" %></th>
        <td><%= f.text_field :name, autofocus: true %></td>
      </tr>
    </table>
      <div><%= f.submit "変更を保存", class:"btn btn-success" %></div>
    <% end %>
  </div>
  
</div>
