<div class="container">
  
  <div class="row mt-3">
    <h2>タグ一覧</h2>
  </div>
  
  <div class="row mt-1">
    <table>
      <thead>
        <tr>
          <th>タグ名</th>
        </tr>
      </thead>
      <% @tags.each do |tag| %>
      <tbody>
        <tr>
          <td><%= link_to tag.name, edit_admin_tag_path(tag.id), class:"text-dark" %></td>
        </tr>
      </tbody>
      <% end %>
    </table>
  </div>
  
  <div class="row mt-3">
    <h2>タグ新規登録</h2>
  </div>
  
  <div class="row mt-1">
    <%= form_with model: @tag, url: admin_tags_path, local: true do |f| %>
    <table>
      <tr>
        <th><%= f.label "タグ名" %></th>
        <td><%= f.text_field :name %></td>
      </tr>
    </table>
    <div class="mt-3"><%= f.submit "保存", class:"btn btn-success" %></div>
    <% end %>
  </div>
  
</div>
