<% flash.each do |key, value| %>
  <% 
    case key 
    when "notice" then
        key = "info"
    when "alert" then
        key = "danger"
    end 
  %>
  <%= content_tag(:p, value, class: "alert alert-#{key}") %>
<% end %>
