<div class="container">
  
  <div class="row mt-3">
    <h2>カテゴリ一覧</h2>
  </div>
  
  <div class="row mt-3">
    <table>
      <thead>
        <tr>
          <th>カテゴリ名</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <% @categories.each do |category| %>
        <tr>
          
          <td><%= link_to category.name,foods_path(category_id: category.id), class:"text-dark" %></td>
          
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
  
</div>
