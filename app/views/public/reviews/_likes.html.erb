<% if review.liked_by?(current_user) %>
  <%= link_to likes_path(review_id: review), method: :delete, class: "like-btn text-dark" do %>
    <i class="fas fa-heart liked" style="color: red;"></i>
    <%= review.likes.count %>
  <% end %>
<% else %>
  <%= link_to likes_path(review_id: review), method: :post, class: "like-btn text-dark" do %>
    <i class="far fa-heart"></i>
    <%= review.likes.count %>
  <% end %>
<% end %>
