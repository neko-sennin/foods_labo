<div class="d-flex justify-content-end">
  <%= form_with url: reviews_path, method: :get, local: true, class: 'boards__searchForm' do |f|%>
    <div class="input-group">
      <%= f.text_field :keyword, value: @keyword, class: "form-control search-control", placeholder: "キーワードを入力" %>
        <span class="input-group-btn input-group-append">
          <%= button_tag type: "submit", class: "btn btn-secondary search-btn" do %>
            <i class="fas fa-search"></i> 検索</submit>
          <% end %>
        </span>
    </div>

      <%= select_tag :tag_id,
        options_from_collection_for_select(Tag.all, :id, :name, params[:tag_id]),
          {
            prompt: "タグで絞り込み検索",
            onchange: "submit(this.form);"
          }
      %>
      <%= select_tag :food_id,
        options_from_collection_for_select(Food.all, :id, :name, params[:Food_id]),
          {
            prompt: "食材で絞り込み検索",
            onchange: "submit(this.form);"
          }
      %>
      
  <% end %>
</div>
